global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prompt-backend'
    static_configs:
      - targets: ['prompt-backend:8000']
    metrics_path: '/metrics'
    scrape_interval: 5s

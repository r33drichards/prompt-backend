global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prompt-backend'
    static_configs:
      # Update this with your actual Railway service URL
      # Format: <service-name>-production.up.railway.app
      - targets: ['prompt-backend-production.up.railway.app']
    scheme: https
    metrics_path: '/metrics'
    scrape_interval: 5s
